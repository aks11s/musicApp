// Figma spec (per-item container): icon 22px + 5px gap + 13px label — 40px total.
// Real device bottom safe-area inset is added on top via useTabBarHeight(), not guessed.
export const TAB_BAR_CONTENT_HEIGHT = 40;
export const TAB_BAR_TOP_PADDING = 13;
export const TAB_BAR_ICON_SIZE = 22;
